# Historical Revenue Note

## Retired Rule

An older draft counted cancelled orders in revenue. That draft is retained only
as historical context and conflicts with the current structured SemanticCatalog
definition. It must not silently override the current catalog.
